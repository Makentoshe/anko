apply from: '../../library.gradle'

dependencies {
    api "androidx.fragment:fragment-ktx:$ANDROIDX_VERSION"
    api "androidx.appcompat:appcompat:$ANDROIDX_VERSION"
    api project(':static:commons-base')
}
